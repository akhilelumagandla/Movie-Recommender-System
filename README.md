# Movie-Recommender-System
A content-based recommendation application built using the TMDB dataset/API. It analyzes movie features such as genres, keywords, cast, and crew to recommend similar movies. The project demonstrates data preprocessing, feature engineering, similarity computation, and an interactive user interface for discovering movies.
